### HTTP
> whenever we make a request to a url, we would use the request in python but its slower comparing to the niquest

`
if i have to comapre request with niquest it would be like a 150cc bike VS 300cc bike
`
### we will be using the niquest instead of request and it uses the same functionality request but different approch to the problem
